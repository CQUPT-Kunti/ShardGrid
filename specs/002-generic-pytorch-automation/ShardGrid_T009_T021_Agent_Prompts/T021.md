# ShardGrid 002 — T021 执行 Prompt

## 当前任务

```text
Feature: 002-generic-pytorch-automation
Task: T021
Title: Phase 1 gate
Phase: Phase 1
Priority: P0 BLOCKING
Depends on: T009-T020
Goal: PLANNER_COMPATIBILITY=PASS
```

# 执行协议（必须遵守）

你是 **实现 Agent**，不是架构设计 Agent。本文件已经给出当前任务的实现边界和验收方式；不要重新设计 002 Feature，不要自行扩大范围。

执行规则：

1. 只执行本文件指定的一个 Task；成功后停止，不要自动开始下一个 Task。
2. 先确认依赖 Task 已经存在于当前分支；依赖不满足则 `STATUS=BLOCKED`，停止。
3. 只修改本任务列出的生产/测试文件；确实需要额外文件时，只有“机械性的 import/export/fixture 引用”可以做，任何架构扩展都必须停止并报告。
4. 不修改 `specs/001-*`，不修改 `002` 的 `spec.md / plan.md / tasks.md`，除非本文件明确要求。
5. 不添加 model-name special case、model-specific Stage、`ModelProvider`、用户必须实现的 `sample_inputs()` / `build_model()` / `compute_loss()`。
6. 不把 `named_modules()` 注册顺序当 execution order；也禁止用 `ordered_names = call_order` 这种一次性补丁替代设计。
7. 保持既有原则：Execution Graph、Parameter/Buffer Ownership、Logical Partition、Placement 是不同概念。
8. 不改变 Runtime 的 exact-plan 原则，不引入 Runtime 二次 partition / placement / round-robin。
9. 不引入第二套固定 reserve/headroom 显存准入层。
10. 当前 Task 内的测试失败时，可以在本 Task 文件范围内迭代修复；如果必须依赖后续 Task 才能正确解决，则停止为 `BLOCKED`，不要作弊绕过。
11. 禁止为了 PASS 随意降低 assertion、增加宽泛 xfail、吞异常或把语义错误伪装成 `NO_FEASIBLE_PLAN`。

## Git 规则

Task **PASS 后自动 commit 并 push**，不需要询问用户：

- 提交前运行 `git status --short` 和 `git diff`，确保只 stage 当前 Task 相关修改。
- 不使用 `git add .` / `git add -A` 混入无关改动。
- commit message 必须使用中文，并明确包含当前 Task ID，例如：`feat(planner): T009 ...`。
- commit 内容由你根据真实改动自行总结，禁止声称未完成的功能。
- commit 后执行 `git push`；若当前分支没有 upstream，则 `git push -u origin <当前分支>`。
- 禁止 force push，禁止切换到其他业务分支。
- 如果 `FAIL/BLOCKED`：`COMMIT=NO`、`PUSH=NO`。

## 最终汇报格式

```text
TASK=<当前Task>
STATUS=PASS/FAIL/BLOCKED
FILES_CHANGED=<实际文件>
TEST_COMMAND=<实际命令>
TEST_RESULT=<实际结果>
COMMIT=YES/NO
COMMIT_HASH=<如有>
COMMIT_MESSAGE=<中文且包含Task ID>
PUSH=YES/NO
NEXT_TASK_STARTED=false
```


# 本任务性质

T021 是 **Phase 1 Gate**，不是新的架构实现任务。它只验证 T009-T020 的 Planner semantic repair 已经形成一致闭环。不要开始 Production Model Decoupling，不要执行 T022。

# 前置条件

先确认当前分支已经包含 T009-T020 的成功提交；任何一个任务未完成则：

```text
STATUS=BLOCKED
COMMIT=NO
PUSH=NO
```

# Gate 必须验证的 invariant

```text
Execution Graph semantics
State Ownership
ModelProfile execution/state accounting
Logical Partition coverage
Partition validation
Final Plan validation
Placement memory compatibility
Worker Ownership compilation
Runtime stable graph-ID mapping
CPU Planner compatibility matrix
```

必须确认：

- execution graph 与 module registration order 解耦；
- parameter/buffer state ownership 独立且完整；
- exact node coverage / state ownership / boundary values 都被验证；
- placement 使用 fresh free-memory 语义；
- Runtime ownership/partition mapping 严格消费 Planner plan，不重分区/重放置；
- 普通 CPU PyTorch fixture matrix 通过。

# 必须运行的 Gate

```bash
pytest tests/unit/test_generic_planner_compatibility.py tests/unit/test_partition_candidates.py tests/unit/test_planning_contract.py tests/unit/test_dag_runtime.py
```

如果上面 PASS，再运行 Phase 1 直接相关回归：

```bash
pytest tests/unit/test_generic_graph_ir.py tests/unit/test_model_profile_memory.py tests/unit/test_joint_partition_placement.py
```

# Gate 判定

只有以下情况允许：

```text
PLANNER_COMPATIBILITY=PASS
```

- 所有 supported model/invariant 测试 PASS；
- 只有任务/contract 明确规定的 unsupported case；
- 无未知 failure；
- 无意外 XPASS；
- 无宽泛 xfail；
- 无 model-specific patch；
- 无 Runtime re-partition/re-placement。

如果 Gate 暴露的是 T009-T020 的真实回归，可以在 Phase 1 相关文件内修复并重新跑完整 Gate；不要跨入 T022。

# Commit / Push

- 如果为了修复 Gate 回归产生了 T021 范围内的代码/测试修改：PASS 后按协议中文 commit（必须含 `T021`）并 push。
- 如果 Gate 一次通过且 **没有任何文件变化**：不要为了制造 commit 人工修改文件，报告 `COMMIT=NOT_REQUIRED`、`PUSH=NOT_REQUIRED`。
- FAIL/BLOCKED：不 commit、不 push。

# 最终汇报

```text
TASK=T021
STATUS=PASS/FAIL/BLOCKED
PLANNER_COMPATIBILITY=PASS/FAIL
GENERIC_GRAPH=PASS/FAIL
STATE_OWNERSHIP=PASS/FAIL
PARTITION_VALIDATION=PASS/FAIL
FINAL_PLAN_VALIDATION=PASS/FAIL
PLACEMENT=PASS/FAIL
WORKER_OWNERSHIP=PASS/FAIL
RUNTIME_GRAPH_MAPPING=PASS/FAIL
CPU_MODEL_MATRIX=PASS/FAIL
TEST_RESULT=<真实结果>
COMMIT=YES/NO/NOT_REQUIRED
COMMIT_HASH=<如有>
COMMIT_MESSAGE=<如有，中文且包含 T021>
PUSH=YES/NO/NOT_REQUIRED
T022_STARTED=false
```

完成 T021 后立即停止。

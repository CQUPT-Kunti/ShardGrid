# T051 — Phase 5 checkpoint gate

Phase: Phase 5  
Priority: P0 BLOCKING  
Depends on: T045, T046, T047, T048, T049, T050

## 目标

执行 Generic Checkpoint 最终 Gate，建立：

```text
STANDARD_STATE_DICT_STRICT_LOAD=PASS
```

T051 不做新的大规模架构设计。

## 前置检查

确认 T045-T050 均已完成并 push。

如果任何依赖未 PASS：

```text
STATUS=BLOCKED
COMMIT=NO
PUSH=NO
T052_STARTED=false
```

## Gate 测试

必须执行：

```bash
pytest tests/unit/test_checkpoint_generic_state.py tests/integration/test_generic_checkpoint_reload.py
```

Gate 必须确认：

```text
WORKER_SHARD_SCHEMA=PASS
ORIGINAL_STATE_KEYS_PRESERVED=PASS
PARAMETERS_COMPLETE=PASS
BUFFERS_COMPLETE=PASS
DUPLICATE_STATE_REJECTED=PASS
MISSING_STATE_REJECTED=PASS
STALE_OR_MISMATCHED_SHARDS_REJECTED=PASS
PRODUCTION_MODEL_NAME_RECONSTRUCTION=0
STANDARD_STATE_DICT_STRICT_LOAD=PASS
OPTIMIZER_STATE_MERGE=OUT_OF_SCOPE
```

不允许未知 failure 或 unexpected xpass。

## 任务状态记录

如果项目任务流程要求更新 Phase 5 Gate evidence，只允许更新 T051 对应状态/evidence，不修改其他 Task 定义。

如果 T051 纯执行 Gate 且没有任何文件变化：

```text
COMMIT=NOT_REQUIRED
PUSH=NOT_REQUIRED
```

不要为了制造 commit 人工修改无关文件。

## 最终停止

T051 PASS 后立即停止：

```text
T052_STARTED=false
```

禁止进入 Failure Taxonomy / Stress Validation。


## 执行与 Git 规则

- 本轮只执行当前 Task，不提前开始下一个 Task。
- 实现前先检查依赖 Task 是否已 PASS；依赖未满足则 `STATUS=BLOCKED`，不自行补做前置 Task。
- 以当前仓库真实代码为准；如果 Task 文档与真实代码存在阻断性差异，停止并报告，不要伪造实现。
- 不修改 `001-*`，不修改 `002` 的 `spec.md / plan.md / tasks.md`，不修改本目录中的 Task Prompt。
- 不覆盖、reset、删除用户已有未提交修改。
- 提交前执行 `git status --short` 和 `git diff`，只 stage 当前 Task 的改动。
- 当前 Task PASS 后自动 commit，commit message 必须使用中文并明确包含当前 Task ID。
- commit 成功后直接 `git push`；无 upstream 时使用 `git push -u origin <当前分支>`。
- 禁止 force push。
- FAIL/BLOCKED/未知测试失败时：`COMMIT=NO`、`PUSH=NO`、`NEXT_TASK_STARTED=false`。
- 不允许通过降低 assertion、随意增加 xfail、隐藏错误或临时 hack 强行 PASS。
- 当前 Task 完成后输出：Task、状态、测试结果、修改文件、commit hash/message、push 结果。

# T025 — Preserve generic DAG example without production dependency

执行 Feature `002-generic-pytorch-automation` 的 **T025**。只做 T025，不开始 T026。

## 任务定义

- Phase: Phase 2
- Priority: P2
- Depends on: T022
- Files:
  - `examples/models/train_generic_dag.py`
  - `tests/unit/test_train_automatic_plan.py`
  - `tests/unit/test_job_manager_model_coupling.py`
- Goal: 保留现有 generic DAG example 作为 regression/validation asset，同时证明 production code 不再调用它。

## 实现要求

1. 确认 T022 PASS。
2. 保持 `examples/models/train_generic_dag.py` 可独立运行现有 example/test 用例。
3. 明确它属于：

```text
example / regression / validation only
```

4. 如果测试需要，可以增加最小注释、warning、compatibility label，但不要改造成新的生产 API。
5. 测试必须证明 production JobManager generic path 不再把它当 runtime entrypoint。
6. Zoo builder 在这个 example 文件里可以继续存在，因为本任务不是删除测试资产。

## 禁止

- 不要删除 example runtime。
- 不要把它重写成 Phase 4 generic production runtime。
- 不要增加用户必须遵循的 wrapper/API。
- 不要把 production path 再指回这个脚本。

## 测试

```bash
pytest tests/unit/test_train_automatic_plan.py tests/unit/test_job_manager_model_coupling.py
```

Acceptance：

```text
EXAMPLE_RUNTIME_RUNNABLE=PASS
PRODUCTION_INVOCATION_OF_EXAMPLE_RUNTIME=false
```

## Git

PASS 后只提交 T025，commit 中文且必须包含 `T025`，随后 push。
FAIL/BLOCKED：不 commit、不 push。

## 最终汇报

```text
TASK=T025
STATUS=PASS/FAIL/BLOCKED
EXAMPLE_RUNTIME_RUNNABLE=PASS/FAIL
PRODUCTION_DEPENDENCY=false/true
TEST_RESULT=<真实结果>
COMMIT=<hash/NO>
PUSH=PASS/NO
T026_STARTED=false
```

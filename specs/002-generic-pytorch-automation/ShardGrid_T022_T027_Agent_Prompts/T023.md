# T023 — Feed generic planner from captured context

执行 Feature `002-generic-pytorch-automation` 的 **T023**。只做 T023，不开始 T024。

## 任务定义

- Phase: Phase 2
- Priority: P0 BLOCKING
- Depends on: T022
- Files:
  - `src/shardgrid/control/job_manager.py`
  - `tests/unit/test_job_manager_model_coupling.py`
  - `tests/unit/test_generic_planner_compatibility.py`
- Goal: production automatic planning 使用 captured context、`GenericGraphIR`、ownership model 和 `ModelProfile`，不再通过模型类型构造 workload。
- Acceptance: 非 Zoo 模型无需模型名或 catalog entry 即可进入 planning。

## 实现要求

1. 确认 T022 PASS。
2. 把 T022 建立的 generic workload path 接到现有 automatic planner：

```text
captured context
-> GenericGraphIR
-> ownership/state model
-> ModelProfile
-> partition/placement search
```

3. production path 不允许 fallback 到 `_planner_workload()` 的 model-type 分支。
4. `_planner_workload()` 可以继续存在，但只能服务 legacy/example configs。
5. 必须复用 T009-T021 已完成的 planner semantic contracts，不重新设计第二套 graph/profile/ownership 数据模型。
6. 非 Zoo fixture 必须能证明：planning 不需要 `model.type`、`zoo_model`、`build_zoo_model()`、`make_zoo_sample()`。

## 禁止

- 不要实现 `shardgrid run` CLI。
- 不要实现 entrypoint capture bootstrap。
- 不要修改 runtime launch command；这是 T024。
- 不要新增 model-specific planner branch。

## 测试

```bash
pytest tests/unit/test_job_manager_model_coupling.py tests/unit/test_generic_planner_compatibility.py
```

要求：

```text
NON_ZOO_PLANNING=PASS
MODEL_NAME_REQUIRED=false
CATALOG_ENTRY_REQUIRED=false
```

## Git

PASS 后只提交 T023，中文 commit 必须包含 `T023`，然后 `git push`。
FAIL/BLOCKED：不 commit、不 push。

## 最终汇报

```text
TASK=T023
STATUS=PASS/FAIL/BLOCKED
CAPTURED_CONTEXT_TO_PLANNER=PASS/FAIL
NON_ZOO_PLANNING=PASS/FAIL
MODEL_NAME_REQUIRED=false/true
TEST_RESULT=<真实结果>
COMMIT=<hash/NO>
PUSH=PASS/NO
T024_STARTED=false
```

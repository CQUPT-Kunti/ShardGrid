# T027 — Phase 2 production decoupling gate

执行 Feature `002-generic-pytorch-automation` 的 **T027**。这是 Phase 2 最终 Gate。完成后停止，不开始 T028。

## 任务定义

- Phase: Phase 2
- Priority: P0 BLOCKING
- Depends on: T022, T023, T024, T025, T026
- Files:
  - `tests/unit/test_job_manager_model_coupling.py`
  - `src/shardgrid/control/job_manager.py`
- Goal: 建立 `PRODUCTION_ZOO_DEPENDENCY=0`。

## 前置条件

必须先确认：

```text
T022=PASS
T023=PASS
T024=PASS
T025=PASS
T026=PASS
```

否则：`STATUS=BLOCKED`，不 commit、不 push。

## Gate 要求

测试和必要的 gate assertion 必须证明 **production generic path**：

```text
不依赖 training_config.model.type 作为 workload source
不依赖 zoo_model 作为 workload source
不调用 build_zoo_model()
不调用 make_zoo_sample()
不选择 examples/models/train_generic_dag.py 作为 production runtime
```

允许这些内容继续存在于：

```text
examples/
tests/
benchmarks/
stress validation/
explicit legacy compatibility paths
```

不要错误要求整个 repository 零 Zoo 引用。

## 额外验证

确认 legacy/example paths 仍受测试保护，同时 generic production path 能从 T022/T023 的 captured/generic workload contract 进入 planning，并由 T024 产生 generic runtime bootstrap command。

## 禁止

- 不要为了 Gate 删除 Zoo assets。
- 不要实现 `shardgrid run`，那是 T028+。
- 不要开始 entrypoint capture。
- 不要改变 Planner Phase 1 已通过的 semantics。
- 不要使用源码全仓库字符串扫描替代 production-path 精确断言。

## 测试

必须运行：

```bash
pytest tests/unit/test_job_manager_model_coupling.py
```

建议同时运行本阶段直接相关回归：

```bash
pytest \
  tests/unit/test_job_manager_model_coupling.py \
  tests/unit/test_generic_planner_compatibility.py \
  tests/unit/test_memory_probe_launch.py \
  tests/unit/test_train_automatic_plan.py
```

只有无未知失败时：

```text
PRODUCTION_ZOO_DEPENDENCY=0
```

## Git / Push

如果 Gate 需要更新测试/evidence，只提交 T027 相关修改。
Commit 必须中文且包含 `T027`，然后 push。

如果 T027 只是运行 Gate 且没有任何合理文件变化：不要人为修改文件制造 commit；报告 `COMMIT=NOT_REQUIRED`、`PUSH=NOT_REQUIRED`。

FAIL/BLOCKED 时不得 commit/push。

## 最终汇报

```text
TASK=T027
STATUS=PASS/FAIL/BLOCKED
PRODUCTION_ZOO_DEPENDENCY=0/NOT_ZERO
MODEL_TYPE_AS_WORKLOAD_SOURCE=false/true
ZOO_MODEL_AS_WORKLOAD_SOURCE=false/true
BUILD_ZOO_MODEL_IN_PRODUCTION=false/true
MAKE_ZOO_SAMPLE_IN_PRODUCTION=false/true
EXAMPLE_RUNTIME_IN_PRODUCTION=false/true
LEGACY_EXAMPLES_STILL_RUN=PASS/FAIL
TEST_RESULT=<真实结果>
COMMIT=<hash/NOT_REQUIRED/NO>
PUSH=PASS/NOT_REQUIRED/NO
T028_STARTED=false
```

T027 PASS 后立即停止。

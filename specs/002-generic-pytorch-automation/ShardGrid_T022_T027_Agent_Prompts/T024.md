# T024 — Launch generic runtime bootstrap from execution plan

执行 Feature `002-generic-pytorch-automation` 的 **T024**。只做 T024，不开始 T025。

## 任务定义

- Phase: Phase 2
- Priority: P0 BLOCKING
- Depends on: T022, T023
- Files:
  - `src/shardgrid/control/job_manager.py`
  - `tests/unit/test_memory_probe_launch.py`
- Goal: production `_launch_command_for_assignment()` 不再选择 `examples/models/train_generic_dag.py`，而是指向 ShardGrid 内部 generic runtime bootstrap，并携带 exact plan/context artifact 路径。

## 实现要求

1. 确认 T022、T023 PASS。
2. 阅读 `_launch_command_for_assignment()` 和 probe/formal launch 共用路径。
3. 为 **production generic path** 生成内部 ShardGrid runtime bootstrap command。
4. launch command 至少要能携带或引用：
   - exact execution/original plan artifact
   - captured/context artifact
   - job/run identifiers（按现有 contract）
5. 保持现有 rank/world/master/CUDA 等 launch semantics，不要在 JobManager 重新决定 placement。
6. `examples/models/train_generic_dag.py` 仍可在 explicit legacy/example config 下使用，但 production generic path 不得选择它。
7. Memory Probe 与 Formal Training 必须使用同一 generic launch family，不能 probe 一套、formal 另一套。

## 禁止

- 不要在 runtime 重新 partition / round-robin placement。
- 不要实现 T025/T026 的 example deprecation工作。
- 不要实现 Phase 3 的 CLI/capture。
- 不要把 generic runtime 放进 `examples/`。

## 测试

```bash
pytest tests/unit/test_memory_probe_launch.py
```

必须验证：

```text
PRODUCTION_EXAMPLE_RUNTIME_DISPATCH=false
GENERIC_RUNTIME_BOOTSTRAP_SELECTED=true
PLAN_ARTIFACT_PROPAGATED=true
CONTEXT_ARTIFACT_PROPAGATED=true
```

## Git

PASS 后：中文 commit 必须包含 `T024`，只提交本任务，随后 push。
FAIL/BLOCKED：不 commit、不 push。

## 最终汇报

```text
TASK=T024
STATUS=PASS/FAIL/BLOCKED
GENERIC_RUNTIME_BOOTSTRAP=PASS/FAIL
EXAMPLE_SCRIPT_IN_PRODUCTION=false/true
PLAN_CONTEXT_PATHS=PASS/FAIL
TEST_RESULT=<真实结果>
COMMIT=<hash/NO>
PUSH=PASS/NO
T025_STARTED=false
```

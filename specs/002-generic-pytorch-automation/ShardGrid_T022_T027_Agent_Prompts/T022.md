# T022 — Replace production planner workload source

执行 Feature `002-generic-pytorch-automation` 的 **T022**。只做 T022，不开始 T023。

## 任务定义

- Phase: Phase 2
- Priority: P0 BLOCKING
- Depends on: T021
- Files:
  - `src/shardgrid/control/job_manager.py`
  - `tests/unit/test_job_manager_model_coupling.py`
- Goal: 让 `JobManager` 能从 captured context 进入 planning，而不是依赖 `_planner_workload()` 的 model-type 分支。
- Acceptance: 新 production planning path 达到 `PRODUCTION_ZOO_DEPENDENCY=0`。

## 实现要求

1. 先确认 T021 PASS；否则 `STATUS=BLOCKED`。
2. 阅读 `JobManager.run()`、`_build_automatic_parallel_plan()`、`_planner_workload()` 及 T021 产生的 planner contracts。
3. 增加 **generic captured workload input path**，供后续 `CapturedTrainingContext` / graph / profile 接入。
4. 新 production path 不得调用：
   - `build_zoo_model()`
   - `make_zoo_sample()`
   - `zoo_model` 作为 workload source
   - model catalog name 作为 planning contract
5. 旧 `_planner_workload()` 分支暂时保留，只作为 legacy/example compatibility path，不要本任务直接删除。
6. 不要实现 T023 的完整 captured graph/profile wiring；T022 只建立清晰的新 production workload入口和边界。

## 禁止

- 不要增加新的 `if model_name == ...`。
- 不要创建用户必须实现的 `ModelProvider` / `sample_inputs()`。
- 不要修改 runtime launch、checkpoint、CLI。
- 不要为了兼容旧测试把 Zoo fallback 放回 generic production path。

## 测试

运行：

```bash
pytest tests/unit/test_job_manager_model_coupling.py
```

测试应更新为区分：

```text
legacy/example coupling 可以存在
production generic planning path 不依赖 Zoo/model type
```

## Git

PASS 后：

1. `git status --short`
2. `git diff`
3. 只 stage T022 修改
4. 中文 commit，必须包含 `T022`
5. `git push`

失败或 BLOCKED：`COMMIT=NO`，`PUSH=NO`。

## 最终汇报

```text
TASK=T022
STATUS=PASS/FAIL/BLOCKED
GENERIC_PLANNING_INPUT_PATH=PASS/FAIL
PRODUCTION_ZOO_DEPENDENCY=0/NOT_ZERO
LEGACY_PATH_PRESERVED=true/false
TEST_RESULT=<真实结果>
COMMIT=<hash/NO>
PUSH=PASS/NO
T023_STARTED=false
```

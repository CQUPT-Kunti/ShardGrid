# T026 — Keep legacy automatic path behind compatibility tests

执行 Feature `002-generic-pytorch-automation` 的 **T026**。只做 T026，不开始 T027。

## 任务定义

- Phase: Phase 2
- Priority: P2
- Depends on: T022
- Files:
  - `examples/models/train_automatic_plan.py`
  - `tests/unit/test_train_automatic_plan.py`
- Goal: 保留现有 model-specific automatic example 行为，但明确它是 legacy/example compatibility path，不能定义 production generic behavior。

## 实现要求

1. 确认 T022 PASS。
2. 保持现有 `train_automatic_plan.py` example 回归能力。
3. 通过最小 deprecation/compatibility 标记或测试语义明确：

```text
legacy/example only
not production generic contract
```

4. 不删除当前 example 行为；后续 final deprecation gates 再决定生命周期。
5. 不允许 production JobManager 因兼容需求重新依赖该脚本或其 model-specific branch。

## 禁止

- 不要把 legacy script 改造成正式 generic runtime。
- 不要新增新的模型类型分支。
- 不要删除旧 example 以“消除耦合”。
- 不要开始 T027 Gate 修改之外的工作。

## 测试

```bash
pytest tests/unit/test_train_automatic_plan.py
```

Acceptance：

```text
LEGACY_EXAMPLE_COMPATIBILITY=PASS
PRODUCTION_CONTRACT_DEFINED_BY_LEGACY=false
```

## Git

PASS 后只提交 T026，中文 commit 必须包含 `T026`，随后 push。
FAIL/BLOCKED：不 commit、不 push。

## 最终汇报

```text
TASK=T026
STATUS=PASS/FAIL/BLOCKED
LEGACY_EXAMPLE=PASS/FAIL
LEGACY_SCOPE=example-only/incorrect
TEST_RESULT=<真实结果>
COMMIT=<hash/NO>
PUSH=PASS/NO
T027_STARTED=false
```

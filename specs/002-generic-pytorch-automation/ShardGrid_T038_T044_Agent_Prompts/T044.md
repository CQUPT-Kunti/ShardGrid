# T044 — Phase 4 Generic Runtime Gate

Feature: `002-generic-pytorch-automation`

```text
T044 [US3] — Run Phase 4 runtime gate
Phase: Phase 4
Priority: P0 BLOCKING
Depends on: T038, T039, T040, T041, T042, T043
```

## 目标

这是 Phase 4 最终 Gate。必须建立：

```text
GENERIC_RUNTIME_LOCAL=PASS
PLAN_RUNTIME_CONSISTENCY_CHECK=PASS
```

T044 不再重新设计 Runtime；它负责验证 T038-T043 已形成完整、相互一致的 local Generic Runtime。

## 前置检查

确认 T038-T043 均已 PASS，并且对应 commit/push 已完成。任何依赖未完成：

```text
STATUS=BLOCKED
COMMIT=NO
PUSH=NO
```

## Gate 测试

必须运行：

```bash
pytest tests/unit/test_runtime_exact_plan.py tests/integration/test_generic_runtime_local.py
```

必要时可以额外运行 T042/T043 的直接相关回归，但不得用真实硬件 stress 代替本 Gate。

## Gate 必须证明

```text
Runtime consumes exact Planner partition IDs = PASS
Worker ownership matches Planner = PASS
No runtime repartition = PASS
No runtime placement recomputation = PASS
No rank-local round-robin = PASS
Owned-only state materialization = PASS
Local forward/backward/optimizer progress = PASS
Local checkpoint shard evidence = PASS
PLAN_RUNTIME_CONSISTENCY_CHECK = PASS
GENERIC_RUNTIME_LOCAL = PASS
```

未知 failure、unexpected xpass、靠临时 xfail 隐藏的问题均不允许通过 Gate。

## 文件更新

如果当前 spec-kit 工作流要求记录 Gate evidence，只允许更新 T044 完成状态/evidence；不要修改后续 T045+ 定义。若没有文件需要修改，不要为了制造 commit 人工修改无关文件。

## 成功后的停止点

T044 PASS 后立即停止：

```text
T045_STARTED=false
```

Phase 5 Generic Checkpoint 由下一批任务单独执行。

## Git / 提交规则

完成实现后必须运行任务指定测试，并执行：

```bash
git status --short
git diff
```

只提交当前 Task 的修改，不要混入用户已有或其他 Task 的改动；不要无脑 `git add .` / `git add -A`。

只有当前 Task **PASS** 时才自动 commit + push：
- commit message 必须使用中文；
- commit message 必须明确包含当前 Task ID；
- 内容根据真实改动自行总结；
- commit 成功后直接 `git push`；无 upstream 时使用 `git push -u origin <当前分支>`；
- 禁止 force push。

如果任务 FAIL / BLOCKED / 出现无法解释的 regression：

```text
COMMIT=NO
PUSH=NO
NEXT_TASK_STARTED=false
```

立即停止，不要跳过当前任务，不要降低断言，不要随意增加 xfail，不要提前实施下一个 Task。

## 最终汇报

```text
TASK=T044
STATUS=PASS/FAIL/BLOCKED
TEST_RESULT=<真实结果>
FILES_CHANGED=<实际文件>
PRODUCTION_BEHAVIOR_CHANGED=<true/false>
NEXT_TASK_STARTED=false
COMMIT=YES/NO
COMMIT_HASH=<如有>
COMMIT_MESSAGE=<中文且包含 T044>
PUSH=YES/NO
PUSH_RESULT=<真实结果>
```

完成当前 Task 后停止；由外层 Agent 再读取下一个 Task 文件。

# T031 执行 Prompt

执行 ShardGrid Feature：

```text
002-generic-pytorch-automation
```

本轮 **只执行 T031**。完成后按规则 commit + push，然后停止；不要提前实施 T032。

项目根目录：

```text
/home/yangjilei/Code/ShardGrid
```

开始前：

```bash
cd /home/yangjilei/Code/ShardGrid
```

## T031 — Test first-step capture contract

```text
Phase: Phase 3
Priority: P0 BLOCKING
Depends on: T030
Files:
- tests/integration/test_entrypoint_capture.py
- tests/fixtures/ordinary_training_scripts/
```

目标：在 bootstrap 实现前，用测试固定 `CapturedTrainingContext` 合同。

测试必须覆盖 `contracts/capture.md` 的必需信息：

```text
entrypoint path / argv / cwd / environment summary
capture backend + version evidence
model identity / class-module evidence
parameter count / buffer count
first usable batch structure
model positional args / keyword args
tensor shape / dtype / device / requires_grad metadata
optimizer class / param-group structure / hyperparameters
loss / backward / optimizer / scheduler boundary evidence
original state_dict key -> canonical state ID mapping
unsupported capability diagnostics
```

重点要求：

1. 不只测试 `model(x)`；必须使用 T030 中的 structured input fixtures；
2. batch/args/kwargs 的结构语义必须保留，不允许 flatten 后只剩 Tensor 列表；
3. 捕获发生在 distributed mutation 前；
4. 用户脚本不增加任何 ShardGrid 专用 API；
5. unsupported capture 必须有结构化失败 contract，而不是 generic exception；
6. 本任务是 contract tests，不实现 `runner.py`。

### 测试

```bash
pytest tests/integration/test_entrypoint_capture.py
```

允许针对“bootstrap 尚未实现”的精确 contract expected failure，但不得掩盖 fixture 或合同本身错误。

### 完成条件

```text
CAPTURE_CONTRACT_COVERED=PASS
STRUCTURED_ARGS_KWARGS=PASS
STATE_KEY_MAPPING_CONTRACT=PASS
LIFECYCLE_EVIDENCE_CONTRACT=PASS
T032_STARTED=false
```

## 全阶段硬约束

- 不要求用户实现 `ModelProvider`、`build_model()`、`sample_inputs()`、`compute_loss()` 或 ShardGrid 专用模型接口。
- 普通用户脚本必须保持正常 PyTorch 写法；ShardGrid 通过内部 bootstrap / interception 完成捕获。
- 捕获必须发生在 distributed parameter mutation 之前。
- 必须保留真实 `args/kwargs` 与结构化 batch 语义，不能把输入粗暴扁平化成单 Tensor。
- 现有 `shardgrid train` 在本阶段继续保留。
- 不允许引入 model-name / Zoo special case。
- 不开始下一 Task，直到当前 Task 测试、commit、push 全部成功。

## Git / 提交规则

完成实现后必须运行本任务指定测试，并检查：

```bash
git status --short
git diff
```

只允许提交当前 Task 的修改，不得混入用户已有修改或其他 Task。不要无脑 `git add .` / `git add -A`。

只有当前 Task **PASS** 时才自动 commit + push：

- commit message 必须使用中文；
- commit message 必须明确包含当前 Task ID；
- 根据实际改动自行总结，不得声明未完成能力；
- commit 成功后直接 `git push`；无 upstream 时使用 `git push -u origin <当前分支>`；
- 禁止 force push、改写历史、随意切换分支。

如果 FAIL / BLOCKED / 出现未知回归：

```text
COMMIT=NO
PUSH=NO
NEXT_TASK_STARTED=false
```

立即停止，不得自动进入下一 Task。

## 最终汇报
```text
TASK=T031
STATUS=PASS/FAIL/BLOCKED
TEST_RESULT=<真实结果>
CAPTURE_CONTRACT_COVERED=PASS/FAIL
STRUCTURED_ARGS_KWARGS=PASS/FAIL
LIFECYCLE_EVIDENCE_CONTRACT=PASS/FAIL
COMMIT=YES/NO
COMMIT_HASH=<如有>
COMMIT_MESSAGE=<中文且包含 T031>
PUSH=YES/NO
T032_STARTED=false
```

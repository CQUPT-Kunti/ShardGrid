# T037 执行 Prompt

执行 ShardGrid Feature：

```text
002-generic-pytorch-automation
```

本轮 **只执行 T037**。完成后按规则 commit + push，然后停止；不要提前实施 T038。

项目根目录：

```text
/home/yangjilei/Code/ShardGrid
```

开始前：

```bash
cd /home/yangjilei/Code/ShardGrid
```

## T037 — Phase 3 capture gate

```text
Phase: Phase 3
Priority: P0 BLOCKING
Depends on: T028, T029, T030, T031, T032, T033, T034, T035, T036
Files:
- tests/integration/test_run_cli.py
- tests/integration/test_entrypoint_capture.py
```

目标：完成 Phase 3 Gate：

```text
ENTRYPOINT_CAPTURE=PASS
```

T037 原则上不增加新的大架构，只负责验证 T028-T036 已经形成完整入口捕获链。

开始前必须确认：

```text
T028=PASS
T029=PASS
T030=PASS
T031=PASS
T032=PASS
T033=PASS
T034=PASS
T035=PASS
T036=PASS
```

Gate 必须证明：

1. `shardgrid run [options] train.py [args...]` CLI contract 正确；
2. 用户 argv/cwd/env 保留；
3. 普通 PyTorch scripts **零 ShardGrid model changes**；
4. 能捕获真实 model、first batch、args/kwargs、structured tensor metadata；
5. 能捕获 state_dict key mapping；
6. capture-context artifact 可持久化并由 JobManager 加载；
7. production planning 不回退 model-name/Zoo workload；
8. export/FX unsupported 情况有结构化诊断；
9. optimizer/lifecycle metadata 足够支持或明确拒绝后续 Runtime；
10. unsupported 情况全部在 distributed mutation 前停止；
11. 现有 `shardgrid train` 没有被破坏。

### Gate 测试

```bash
pytest tests/integration/test_run_cli.py tests/integration/test_entrypoint_capture.py
```

不允许：

- 未知 failure；
- 为了 Gate 临时新增 xfail；
- unexpected xpass 未调查；
- 修改 production 行为来绕过测试；
- 在 Gate 中提前实现 T038 Runtime。

只有满足全部 Gate 条件才能：

```text
ENTRYPOINT_CAPTURE=PASS
```

如果本任务需要按当前 spec-kit 习惯记录 Gate evidence，只更新与 T037 完成状态/evidence 直接相关的 `002` task 记录；不要重写 spec/plan 或其他 Task。

### 完成条件

```text
ENTRYPOINT_CAPTURE=PASS
ORDINARY_SCRIPT_ZERO_SHARDGRID_MODEL_CHANGES=PASS
CAPTURE_CONTEXT=PASS
CAPTURE_FIRST_PLANNING=PASS
UNSUPPORTED_PRE_MUTATION_FAILURE=PASS
EXISTING_TRAIN_REGRESSION=PASS
T038_STARTED=false
```

## 全阶段硬约束

- 不要求用户实现 `ModelProvider`、`build_model()`、`sample_inputs()`、`compute_loss()` 或 ShardGrid 专用模型接口。
- 普通用户脚本必须保持正常 PyTorch 写法；ShardGrid 通过内部 bootstrap / interception 完成捕获。
- 捕获必须发生在 distributed parameter mutation 之前。
- 必须保留真实 `args/kwargs` 与结构化 batch 语义，不能把输入粗暴扁平化成单 Tensor。
- 现有 `shardgrid train` 在本阶段继续保留。
- 不允许引入 model-name / Zoo special case。
- 不开始下一 Task，直到当前 Task 测试、commit、push 全部成功。

## Git / 提交规则

完成实现后必须运行本任务指定测试，并检查：

```bash
git status --short
git diff
```

只允许提交当前 Task 的修改，不得混入用户已有修改或其他 Task。不要无脑 `git add .` / `git add -A`。

只有当前 Task **PASS** 时才自动 commit + push：

- commit message 必须使用中文；
- commit message 必须明确包含当前 Task ID；
- 根据实际改动自行总结，不得声明未完成能力；
- commit 成功后直接 `git push`；无 upstream 时使用 `git push -u origin <当前分支>`；
- 禁止 force push、改写历史、随意切换分支。

如果 FAIL / BLOCKED / 出现未知回归：

```text
COMMIT=NO
PUSH=NO
NEXT_TASK_STARTED=false
```

立即停止，不得自动进入下一 Task。

## T037 特殊提交规则

如果 Gate PASS 且产生了 T037 自身必要的 evidence/task-state 修改，则按普通规则中文 commit（包含 `T037`）并 push。

如果 Gate PASS 且 **没有任何需要提交的文件变化**，不要为了制造 commit 修改无关文件：

```text
COMMIT=NOT_REQUIRED
PUSH=NOT_REQUIRED
```

然后停止。

## 最终汇报
```text
TASK=T037
STATUS=PASS/FAIL/BLOCKED
TEST_RESULT=<真实结果>
ENTRYPOINT_CAPTURE=PASS/FAIL
ORDINARY_SCRIPT_ZERO_SHARDGRID_MODEL_CHANGES=PASS/FAIL
CAPTURE_CONTEXT=PASS/FAIL
UNSUPPORTED_PRE_MUTATION_FAILURE=PASS/FAIL
EXISTING_TRAIN_REGRESSION=PASS/FAIL
COMMIT=YES/NO/NOT_REQUIRED
COMMIT_HASH=<如有>
COMMIT_MESSAGE=<如有，中文且包含 T037>
PUSH=YES/NO/NOT_REQUIRED
T038_STARTED=false
```

T037 PASS 后立即停止，禁止开始 T038。

# T029 执行 Prompt

执行 ShardGrid Feature：

```text
002-generic-pytorch-automation
```

本轮 **只执行 T029**。完成后按规则 commit + push，然后停止；不要提前实施 T030。

项目根目录：

```text
/home/yangjilei/Code/ShardGrid
```

开始前：

```bash
cd /home/yangjilei/Code/ShardGrid
```

## T029 — Add run command shell

```text
Phase: Phase 3
Priority: P0 BLOCKING
Depends on: T028
Files:
- src/shardgrid/cli/app.py
- src/shardgrid/cli/commands/run.py
- tests/integration/test_run_cli.py
```

目标：正式注册 `shardgrid run`，但只建立入口 shell，不提前实现后续 capture bootstrap。

必须实现合同：

```bash
shardgrid run [--config CLUSTER_CONFIG] [--dry-run] [--json] ENTRYPOINT [ARGS...]
```

实现要求：

1. 在 `src/shardgrid/cli/app.py` 注册 `run`，保持现有 `train` 完全可用；
2. 新建/实现 `src/shardgrid/cli/commands/run.py`；
3. 将解析结果组织为 plan 中的 `TrainingEntrypoint` 语义：entrypoint path、argv、cwd/environment 入口信息、cluster config、dry-run、json flag；内部类型名按当前代码最小侵入实现；
4. **用户脚本 argv 必须逐项、逐顺序保留**；`train.py --config x` 中的 `--config` 不能被误当成 ShardGrid config；
5. 不要求 world_size、GPU count、host count、stage count、min-workers；
6. 不实现用户模型 factory/sample API；
7. 本任务只把 `TrainingEntrypoint` 风格对象交给控制层 seam，真正 bootstrap 在 T032；
8. 不改现有 `shardgrid train` 行为。

### 测试

```bash
pytest tests/integration/test_run_cli.py tests/unit/test_cli_registration.py
```

必须验证：

```bash
shardgrid run --config cluster.yaml train.py --config config.yaml
```

解析正确。

### 完成条件

```text
RUN_COMMAND_REGISTERED=PASS
ARGV_PASSTHROUGH=PASS
TRAIN_COMMAND_REGRESSION=PASS
NO_RESOURCE_COUNT_REQUIRED=true
T030_STARTED=false
```

## 全阶段硬约束

- 不要求用户实现 `ModelProvider`、`build_model()`、`sample_inputs()`、`compute_loss()` 或 ShardGrid 专用模型接口。
- 普通用户脚本必须保持正常 PyTorch 写法；ShardGrid 通过内部 bootstrap / interception 完成捕获。
- 捕获必须发生在 distributed parameter mutation 之前。
- 必须保留真实 `args/kwargs` 与结构化 batch 语义，不能把输入粗暴扁平化成单 Tensor。
- 现有 `shardgrid train` 在本阶段继续保留。
- 不允许引入 model-name / Zoo special case。
- 不开始下一 Task，直到当前 Task 测试、commit、push 全部成功。

## Git / 提交规则

完成实现后必须运行本任务指定测试，并检查：

```bash
git status --short
git diff
```

只允许提交当前 Task 的修改，不得混入用户已有修改或其他 Task。不要无脑 `git add .` / `git add -A`。

只有当前 Task **PASS** 时才自动 commit + push：

- commit message 必须使用中文；
- commit message 必须明确包含当前 Task ID；
- 根据实际改动自行总结，不得声明未完成能力；
- commit 成功后直接 `git push`；无 upstream 时使用 `git push -u origin <当前分支>`；
- 禁止 force push、改写历史、随意切换分支。

如果 FAIL / BLOCKED / 出现未知回归：

```text
COMMIT=NO
PUSH=NO
NEXT_TASK_STARTED=false
```

立即停止，不得自动进入下一 Task。

## 最终汇报
```text
TASK=T029
STATUS=PASS/FAIL/BLOCKED
TEST_RESULT=<真实结果>
RUN_COMMAND_REGISTERED=PASS/FAIL
ARGV_PASSTHROUGH=PASS/FAIL
TRAIN_COMMAND_REGRESSION=PASS/FAIL
COMMIT=YES/NO
COMMIT_HASH=<如有>
COMMIT_MESSAGE=<中文且包含 T029>
PUSH=YES/NO
T030_STARTED=false
```
